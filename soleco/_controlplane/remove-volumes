#!/usr/bin/env bash



  ######################################################
  # THIS FILE IS AUTO-GENERATED - PLEASE DO NOT CHANGE #
  ######################################################

#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_ekg_elasticsearch_volume"
	result=$(docker volume ls | grep "swarm_logs_ekg_elasticsearch_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_ekg_grafana_volume"
	result=$(docker volume ls | grep "swarm_logs_ekg_grafana_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_ingest_container_logs_filebeat_volume"
	result=$(docker volume ls | grep "swarm_logs_ingest_container_logs_filebeat_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_kz_kafka_secrets_volume"
	result=$(docker volume ls | grep "swarm_logs_kz_kafka_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_kz_kafka_data_volume"
	result=$(docker volume ls | grep "swarm_logs_kz_kafka_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_kz_zookeeper_secrets_volume"
	result=$(docker volume ls | grep "swarm_logs_kz_zookeeper_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_kz_zookeeper_data_volume"
	result=$(docker volume ls | grep "swarm_logs_kz_zookeeper_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_kz_zookeeper_log_volume"
	result=$(docker volume ls | grep "swarm_logs_kz_zookeeper_log_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "swarm_logs_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "swarm_logs_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_processor_streamsets_volume"
	result=$(docker volume ls | grep "swarm_logs_processor_streamsets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "swarm_logs_api_rest_elasticsearch_rest_elasticsearch_data_volume"
	result=$(docker volume ls | grep "swarm_logs_api_rest_elasticsearch_rest_elasticsearch_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
